client_max_body_size 10M;

add_header Strict-Transport-Security 'max-age=31536000; includeSubDomains; preload';
add_header X-XSS-Protection "1; mode=block";
add_header X-Frame-Options "SAMEORIGIN";
add_header X-Content-Type-Options nosniff;
add_header Referrer-Policy "strict-origin";
add_header Permissions-Policy "geolocation=(),midi=(),sync-xhr=(),microphone=(),camera=(),magnetometer=(),gyroscope=(),fullscreen=(self),payment=()";
# add_header Content-Security-Policy "default-src 'self'; script-src 'self' https://cdn.segment.com/analytics.js/v1/iCkMy7ymtJ9qYzQRXkQpnAJEq7D4NyMU/analytics.min.js; style-src 'self' https://wrt-titan.ap-south-1.elasticbeanstalk.com/static/css/main.5513bd04.css";

# add_header Content-Security-Policy 
#     default-src 'self';
#     script-src 'self';
#     style-src 'self';
# ";

# http {
#     server {
#         listen       443;  # or 443 if using HTTPS
#         server_name  your_server_name;  # Adjust server name (e.g., domain.com)

#         location /your_endpoint {
#             proxy_pass http://your_backend;  # Adjust this to point to your backend

#             # Set headers to handle method override
#             if ($request_method = POST) {
#                 proxy_set_header X-Http-Method-Override $http_x_http_method_override;
#             }

#             proxy_set_header X-Real-IP $remote_addr;
#             proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#             proxy_set_header Host $host;
#             proxy_set_header X-Forwarded-Proto $scheme;
#         }
#     }
# }